export type ColorKey = 'primary'
  | 'secondary'
  | 'hover'
  | 'click'
  | 'card_background'
  | 'blue_black'
  | 'blue_white'
  | 'blue_light_gray'
  | 'blue_gray';

export interface Colors {
  primary: string;
  secondary: string;
  hover: string;
  click: string;
  card_background: string;
  blue_black: string;
  blue_white: string;
  blue_light_gray: string;
  blue_gray: string;
}